[package]
name = "gallery"
version = "0.1.0"
edition = "2021"
description = "Minimal photo/video gallery viewer built with GPUI"
license = "MIT"

[dependencies]
gpui = "0.2.2"

[profile.release]
lto = true
codegen-units = 1
